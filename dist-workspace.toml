[workspace]
members = ["cargo:crates/linesmith"]

# Config for 'cargo-dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.31.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "powershell", "homebrew"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = [
  "x86_64-apple-darwin",
  "aarch64-apple-darwin",
  "x86_64-unknown-linux-gnu",
  "x86_64-unknown-linux-musl",
  "aarch64-unknown-linux-gnu",
  "x86_64-pc-windows-msvc",
  "aarch64-pc-windows-msvc",
]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Where to host releases
hosting = "github"
# Whether to install an updater program
install-updater = false
# Homebrew tap repository
tap = "oakoss/homebrew-tap"
# Publish the Homebrew formula on release
publish-jobs = ["homebrew"]
# Don't run on PRs (CI handles PR validation)
pr-run-mode = "skip"
# Enable GitHub artifact attestations (SLSA provenance)
github-attestations = true
# Embed dependency tree in binaries for security scanning (cargo audit bin)
cargo-auditable = true
# Allow Renovate-style SHA pin updates in the generated release.yml without
# fighting `dist generate --mode=ci`. Remove temporarily when regenerating.
allow-dirty = ["ci"]

# Use ubuntu-latest so Renovate doesn't create PRs for runner updates
[dist.github-custom-runners]
global = "ubuntu-latest"

# Pin GitHub Actions to specific commit SHAs (supply-chain hardening per
# docs/specs/release-process.md §Supply-chain posture). Renovate keeps these
# current; edit commit SHAs directly here rather than tag names.
[dist.github-action-commits]
"actions/checkout" = "de0fac2e4500dabe0009e67214ff5f5447ce83dd"              # v6.0.2
"actions/upload-artifact" = "043fb46d1a93c77aae656e7c1c64a875d1fc6a0a"       # v7.0.1
"actions/download-artifact" = "3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c"     # v8.0.1
"actions/attest-build-provenance" = "a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32" # v4.1.0
