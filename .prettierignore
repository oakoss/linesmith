LICENSE
target/
Cargo.lock
.beads/
.git/
# Parallel-agent worktrees from claude code: nested copies of the repo
# would otherwise trip prettier on files this config already excludes
# at the root (e.g. .github/workflows/release.yml).
.claude/worktrees/
node_modules/
*.toml
# Auto-generated by `dist generate --mode=ci`; leave as cargo-dist emits it.
.github/workflows/release.yml
# CHANGELOG is owned by release-plz / git-cliff (template emits bare
# underscores in segment names; prettier escapes them). The
# crates/linesmith/ entry is a symlink kept for release-plz path
# resolution.
CHANGELOG.md
crates/linesmith/CHANGELOG.md
# Generated by `mise run schema:update` from crate::config types via
# schemars. Prettier reformatting would diverge the file from the
# generator output and break `mise run schema:check` in CI.
config.schema.json
