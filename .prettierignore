LICENSE
target/
Cargo.lock
.beads/
.git/
node_modules/
*.toml
# Auto-generated by `dist generate --mode=ci`; leave as cargo-dist emits it.
.github/workflows/release.yml
# Symlink to root CHANGELOG.md (kept for release-plz path resolution); prettier
# can't format symlinks.
crates/linesmith/CHANGELOG.md
# Generated by release-plz / git-cliff; the template owns escape conventions
# (e.g. underscores in segment names) and prettier disagrees on every
# regeneration.
CHANGELOG.md
